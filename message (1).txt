echo "=== FULL MAC EXPLOIT CHECK ==="; echo ""; SUS=0; echo ">>> 1. Checking for known exploit apps..."; for app in "Opiumware" "Fleasion" "MacSploit" "Hydrogen" "Delta" "AppleBlox" "ScriptWare" "Script-Ware"; do found=$(find /Applications ~/Applications -iname "*$app*" 2>/dev/null); if [ -n "$found" ]; then echo "FOUND APP → $found"; SUS=1; fi; done; echo ""; echo ">>> 2. Checking for leftover folders & files..."; for path in "$HOME/Opiumware" "$HOME/Library/Application Support/FleasionNT" "$HOME/Library/Application Support/Fleasion" "/Applications/Roblox.app/Contents/MacOS/ClientSettings/ClientAppSettings.json"; do if [ -e "$path" ]; then echo "FOUND → $path"; SUS=1; fi; done; echo ""; echo ">>> 3. Searching home folder for exploit files..."; SEARCH=$(find ~ -iname "*macsploit*" -o -iname "*hydrogen*" -o -iname "*opiumware*" -o -iname "*fleasion*" -o -iname "*executor*" -o -iname "*sploit*" 2>/dev/null | grep -v -E "Caches|Logs|\.Trash|node_modules" | head -15); if [ -n "$SEARCH" ]; then echo "$SEARCH"; SUS=1; else echo "No exploit files found in home"; fi; echo ""; echo ">>> 4. Terminal History (past commands + time)..."; if [ -f ~/.zsh_history ]; then HIST=$(grep -E "Opiumware|Fleasion|MacSploit|Hydrogen|ClientAppSettings|rm -rf.*Opium|rm -rf.*Fleasion|find.*executor|curl.*uninstaller|curl.*Opiumware" ~/.zsh_history 2>/dev/null | tail -15); if [ -n "$HIST" ]; then echo "$HIST"; SUS=1; else echo "No suspicious commands in history"; fi; else echo "No zsh history found"; fi; echo ""; echo ">>> 5. Trash Check (with deletion time)..."; if ls ~/.Trash 2>/dev/null | grep -qiE "opium|fleasion|macsploit|hydrogen|executor|sploit|delta|appleblox"; then echo "Found in Trash:"; ls -lT ~/.Trash 2>/dev/null | grep -iE "opium|fleasion|macsploit|hydrogen|executor|sploit|delta|appleblox"; echo "(Time shown = when it was moved to Trash)"; SUS=1; else echo "Nothing related currently in Trash"; fi; echo ""; echo ">>> 6. Already Deleted Check..."; for path in "/Applications/Opiumware.app" "/Applications/Fleasion.app" "$HOME/Opiumware" "$HOME/Library/Application Support/FleasionNT" "$HOME/Library/Application Support/Fleasion"; do if [ ! -e "$path" ]; then echo "Already deleted → $path"; SUS=1; fi; done; echo ""; echo "========================================"; if [ $SUS -eq 1 ]; then echo "⚠️  RESULT: THIS PERSON IS SUS"; echo "Exploits, leftovers, history, or deleted items were found."; echo "Check the sections above for what was changed and when."; else echo "✅ RESULT: CLEAN"; echo "Nothing suspicious was found."; fi; echo "========================================"